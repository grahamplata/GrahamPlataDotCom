<!-- Bits and Pieces -->
<%= render 'partials/header' %>
<%= render 'partials/specialnav' %>
<!-- Content -->

  <%= @posts.each do |post| %>
    <%= post.title, post %>
    <%= post.created_at.strftime("%A, %b %d") %>
  <% end %>

<!-- Footers -->
<%= render 'partials/footer' %>
